function fetchedData = fetchData(cloudURL)
    % Simulate fetching data from the cloud
    disp(['Fetching data from cloud at ' cloudURL '...']);
    fetchedData = rand(100, 1); % Return random data as an example
end
